## Phase 3 sequence

PRD-10 weeks 12–15 (one engineer; reuses PRD-07 dispatch + PRD-06 patterns heavily) → PRD-11 weeks 14–20 (the portal is the long pole: bidder onboarding + pen-test before first live lot) → PRD-12 weeks 18–22 build, **go-live strictly gated by GP-1**, realistically week 26+ → PRD-13 motor-pack hardening continuous, fire-pack test weeks 16–22 in parallel (per the R4 plan). Nothing here blocks the analyst-equivalence trial, which needs only R2 scope.

## Master open-items register (consolidated ❓ — all discovery/decision, none code-blocking)

| #   | Item                                                                                                                    | Blocks                         | Owner              |
| --- | ----------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ------------------ |
| 1   | Shared mailbox + `svc-pacha-icon`/`svc-pacha-edms` service accounts (in writing, longest lead)                          | ED-5, RPA L2+                  | Aryia → Mayfair IT |
| 2   | DG-1: ICON/EDMS browser-accessibility confirmation; ICON test instance existence                                        | PRD-09 RPA mode                | Aryia + IT         |
| 3   | Click-path recording sessions (1 day × ICON, EDMS) + dropdown value-map screenshots                                     | RPA ops leaving L1             | Aryia + Gilbert    |
| 4   | Rule definitions R-01/R-04/R-16; R-06 threshold (Q-02); R-11 waiver conditions                                          | those rules only               | Aryia (embed)      |
| 5   | C-04; C-07 verbatim; **payable formula** (single highest-value capture — feeds T-01, T-13)                              | PRD-08 L2, PRD-12              | Aryia + CM         |
| 6   | Verbatim templates: T-05, T-08/T-08b, T-09, T-10, T-11, T-12, T-13, T-02b; duty-paid source; re-inspection fee schedule | per-template `pending_capture` | Aryia (embed)      |
| 7   | ≥100-claim anonymised corpus request per §3.6 strata                                                                    | L2 promotions anywhere         | Aryia → CM         |
| 8   | DG-2: EFT form origin/format (mailbox vs drive hook)                                                                    | PRD-12 S8                      | Aryia + Finance    |
| 9   | ODQ-8: bidder KYC depth, bond rules, committee quorum                                                                   | PRD-11 S6/S8 config            | Aryia + HOC        |
| 10  | DV channel (via broker vs direct); statutory settlement clock trigger/duration (legal confirm)                          | PRD-12 S1 config, reporting    | Aryia + legal      |
| 11  | ODPC processor registration + DPIA (incl. af-south-1 transfer + LLM ZDR documentation)                                  | production go-live only        | Aryia              || 12 | **DG-3:** network path AWS ↔ ICON/EDMS (internet + IP allowlist vs LAN-only → on-prem runner host per ED-3a) — **in the same written IT request as item 1** | runner host provisioning only (architecture is host-agnostic per ED-3a) | Aryia → Mayfair IT |
| 13 | HOC approval band: distinct role or ≡ CM at Mayfair | PRD-04 band config | Aryia + CM |
| 14 | Verify the 277,476 figure vs fixture FX-1's reconstruction (139,200 garage door line) with Gilbert | nothing (FX-1 is canonical regardless) | Aryia |
| 15 | Legacy open-book export from ICON (CSV) for ramp-day-0 stub import (trial doc v1.1) | trial ramp day 0 | Aryia + IT |
| 16 | Reg-plate pattern verification against the corpus, week 1 (PRD-01 `kenya_reg` v1.1 set) | thread-match accuracy tuning | Eng + corpus |
| 17 | `icon.reserve_adjust` click-path recording (append to the item-3 recording sessions) | PRD-10 matched_under automation | Aryia + Gilbert |

**v1.1 priority note on item 5:** three captures are prioritised above all template verbatims, in one CM + Gilbert sitting this week: (a) **C-08 payable formula** (feeds T-01, T-13, routing amount); (b) **R-06 threshold** (`assessment.mode_confirm` cannot leave L2 until it lands); (c) **C-07 verbatim, both variants** (write-off claims progress to SALVAGE_BIDDING but not SETTLEMENT until captured).

**v1.1 process rule (ED-11, binding on coding agents):** anything found underdetermined during implementation is routed to **this register** — implement the narrowest safe behaviour (`blocked_on_inputs` / `EXCEPTION` / refuse-to-render) and log the gap here. The register, not the codebase, is where ambiguity goes to die.

## Build-time register additions (ED-11 entries appended during implementation)

| # | Item | Safe behaviour implemented | Raised by |
| --- | --- | --- | --- |
| 18 | ED-1 names the directory `/platform`, which shadows Python's stdlib `platform` module if made an import package | Packages live at `platform/<import_name>/` with pytest `pythonpath`; PRD-00 package import name = `claim_core` (PACKET-01 D-1) | CTO, PACKET-01 |
| 19 | PRD-00 §0.2 defines core-dictionary *coverage* (`policy.*`, `loss.*`, …) but never enumerates the exact path list, types, or PII classes | Packet-1 subset defined in PACKET-01 §5; writes outside it 422 `FIELD_NOT_IN_DICTIONARY`; full dictionary needs a capture session | CTO, PACKET-01 |
| 20 | AuthZ roles are PRD-04 scope but PRD-00 APIs need an actor identity now | Interim `X-Actor` header (`agent:*`/`user:*`/`system`), mandatory; replaced by PRD-04 SSO/RBAC (PACKET-01 D-3) | CTO, PACKET-01 |
| 21 | ED-3 mandates PostgreSQL 16 but no PG instance exists in CI yet | Acceptance tests run SQLAlchemy-on-SQLite by default with `DATABASE_URL` override; PG-only mechanics (`pg_advisory_xact_lock`, `JSONB`) implemented per-dialect; PG-backed CI lands with the infra packet (PACKET-01 D-4) | CTO, PACKET-01 |
| 22 | PRD-00 §0.2 forbids *agents* superseding `human_verified` fields but is silent on a human writing a non-`human_verified` value (e.g. `extracted`) over one | Blocked with the same `409 HUMAN_OVERRIDE_PROTECTED` + review item; a human must re-verify (`human_verified` write) to change it — narrowest safe reading of never-guess | CTO review of PACKET-01 build |
| 23 | Post-triage decline "requires a claims_manager approval item before the transition commits" (PRD-00 §0.4) but the PRD-04 review queue does not exist yet | `202 APPROVAL_REQUIRED` + `review.created{decline_approval_required}` event + `blocked_reasons` gate; claim unchanged; resolution wiring lands with the PRD-04 packet (PACKET-02 §5) | CTO, PACKET-02 |
| 24 | FSM guards referencing unbuilt machinery (R-05, C-02/C-03, manifest, officer sign, R-08, R-13/R-14) cannot be evaluated before PRD-02/03/08 | Structural guards enforced now; rule-linked edges open to explicit API calls with `guards_pending[]` recorded verbatim in the transition event; rule wiring replaces them when the COP runtime lands (PACKET-02 D-6) | CTO, PACKET-02 |
| 25 | DECLINED/WITHDRAWN are "reopenable" (PRD-00 §0.4) but the reopen trigger and target state are only defined by PRD-05 (REOPEN_PROMPT, terminal-state inbound handling) | No reopen endpoint in PACKET-02; `claim.reopened` remains a registered event type; VOID not reopenable (created-in-error) (PACKET-02 D-7) | CTO, PACKET-02 |
| 26 | PRD-00 §0.2 makes `external.*` first-class dictionary paths with source_type `projection_readback` but doesn't state which other source types may write them | Writes restricted to `source_type` ∈ {`projection_readback`, `human`} — agents never invent external refs (PACKET-02 D-8) | CTO, PACKET-02 |
| 27 | PRD-00 gives semantics but no DDL for SLA clock rows or a platform-state store | `sla_clocks` and `platform_state` tables designed locally (PACKET-03 §4.4/§4.5); columns need confirmation at next spec round | CTO, PACKET-03 |
| 28 | "Business hours" bounds for `approval_dwell` never captured; business-*day* definition assumed Mon–Fri excl. KE holidays | `approval_dwell` definition ships `blocked_on_inputs` — engine refuses to start it; business-day assumption recorded, confirm with Mayfair ops | CTO, PACKET-03 |
| 29 | Movable KE public holidays (Eid, Easter-linked) not enumerable as fixed data; some SLA `stop_event`s unstated in §0.5 (e.g. acknowledge) | `holidays.yaml` ships fixed-date statutory holidays only, movables `pending_capture`; unstated stop_events `pending_capture` — clocks stop only via suppression or explicit stop | CTO, PACKET-03 |
| 30 | KMS CMK / S3 Object-Lock unavailable outside AWS | `KeyProvider`/`BlobStore` interfaces with local implementations; KMS + S3 WORM providers land with the infra packet — prod launch blocked until then | CTO, PACKET-03 |
| 31 | ED-6a blind index requires "normalised value" but normalisation rules are unstated | Locally defined: uppercase, strip whitespace/hyphens; phones digits-only with `0`→`254` prefix rewrite — needs capture confirmation before corpus-scale dedupe relies on it | CTO, PACKET-03 |
| 32 | Assorted PACKET-03 micro-decisions: `external_refs` cache uses flat dot-path keys (D-11); ledger `before_hash`/`after_hash` null when payload carries no object state (D-12); `pii.decrypt` appends inline via the single LedgerWriter, not the event spine (D-13) | As described; all reversible without data migration | CTO, PACKET-03 |
| 33 | PRD-01 package naming and module boundary against PRD-00 | `platform/doc_intel/`, import `doc_intel`; cross-package access only via curated `claim_core` root exports (ED-1) | CTO, PACKET-04 |
| 34 | PRD-01 §1.2 "msg/eml→PDF via rendering" gives no fidelity requirement | v1 renders the plaintext body to PDF via PyMuPDF (citation-viable text layer); rich-HTML rendering + nested-attachment handling deferred; `.msg` parsed with extract-msg | CTO, PACKET-04 |
| 35 | No DDL in PRD-01 for per-document pipeline stage state | `document_stages` table designed locally (PACKET-04 §3.1) | CTO, PACKET-04 |
| 36 | §1.7 corpus accuracy gates (97/98/95/99%) need the ≥100-claim corpus (item 7) and the PRD-03 measurement harness — neither exists | Packet tests cover mechanics + §1.7 failure paths only; corpus gates become PRD-03 eval-harness work; do not treat packet-04 green as §1.7 discharge | CTO, PACKET-04 |
| 37 | Doc schemas target `vehicle.*`/`assessment.*` paths that only the motor pack may register, but the PRD-13 pack format doesn't exist yet | Dictionary pack-extension mechanism + interim `packs/motor/fields.yaml` (first /packs artifact); PRD-13 consumes/formalises it | CTO, PACKET-04 |
| 38 | §1.5 doesn't state whether `combined == threshold` commits; FIELD_VERIFY payload content unstated | `>=` commits (boundary inclusive); FIELD_VERIFY carries candidate value in payload, redacted + blob ref for PII paths | CTO, PACKET-04 |
| 39 | PRD-01 names `licence_no` and `phone_ke` validators but gives neither accepted format | Every non-empty value remains `out_of_scope` and routes to FIELD_VERIFY; no value auto-commits until the formats are captured | CTO review, PACKET-05 |
| 40 | PRD-01's “page_count > 4 with mixed page classes” trigger requires page classifications before mixedness is knowable | Run page-level classification for every >4-page document; homogeneous high-confidence pages continue, all mixed/low/other results create DOC_SPLIT | CTO, PACKET-05 |
| 41 | PRD-01 requires `consistency_results` but supplies no DDL | Minimal append-only result schema in PACKET-05 §3.1; no update path, reruns dedupe on the input fingerprint | CTO, PACKET-05 |
| 42 | PRD-01 requires a stored Swahili remarks gloss but supplies no canonical claim-field path | Store `derived/{document_id}/remarks_gloss.json` with `machine_translated:true`, `rule_input:false`, `pending_field_registration`; never commit it to `claim_fields` | CTO, PACKET-05 |
| 43 | CC-2 references “claim_form driver age”, but the claim-form schema contains DOB and years-driving, not driver age | CC-2 writes `insufficient` + CONSISTENCY_FLAG; never substitute years-driving or derive an unstated comparison input | CTO, PACKET-05 |
| 44 | PRD-01 gives p95 SLO thresholds but no aggregation window | Persist every document sample and alert on each individual >3min or >US$0.60 result until PRD-03 defines the p95 series window | CTO, PACKET-05 |
| 45 | DOC_SPLIT human-resolution transport is unspecified before PRD-04 exists | Expose the idempotent engine method in PACKET-05 §3.4; the future PRD-04 resolver calls the same method | CTO, PACKET-05 |
| 46 | PRD-02 package naming and module boundary | `platform/cop_runtime/`, import `cop_runtime`; models on `claim_core.Base`; cross-package access via curated root exports + `app.state` only (ED-1, mirrors #33) | CTO, PACKET-06 |
| 47 | No DDL for `rule_runs`; `calc_runs` has columns but no keys/mechanism; calc input binding unstated | `rule_runs` designed locally (PACKET-06 §3.4); `calc_runs` §2.3 columns binding + `id`/`pack_id`/`pack_version`/`status`/`missing_inputs`/`actor` added; calc input paths declared in the `@calc` decorator | CTO, PACKET-06 |
| 48 | PRD-02 R-02 names `policy.excess_amount` but the core dictionary registered `policy.excess` (PACKET-01 subset); fee/party/salvage/election input paths unnamed by any PRD | Both excess paths exist until a spec round reconciles — no silent remap; missing paths locally named as pack `fields.yaml` extensions (PACKET-06 §3.7), capture confirmation needed | CTO, PACKET-06 |
| 49 | R-07 needs multi-assessor quote paths (PRD-07 FX-1); R-14 needs a discharge-received signal + payee path (PRD-06/PRD-12) — none exist yet | Both slots ship `blocked_on_inputs` naming the missing inputs; unblocked when the owning PRDs register the paths | CTO, PACKET-06 |
| 50 | `pack.late_days` (R-10) value appears nowhere in the doc set | R-10 ships config placeholder + `blocked_on_inputs` — the same treatment §2.2 prescribes for R-06 | CTO, PACKET-06 |
| 51 | R-03 "ex-gratia escalation" has no stated trigger predicate | Fires unconditionally when evaluated; outcome pure `route_review{EX_GRATIA_REVIEW, claims_manager}`; invocation timing owned by the caller (PRD-05 triage); no auto path exists — permanently | CTO, PACKET-06 |
| 52 | §2.2 outcome-verb enum has no transition/selection verb, yet R-05 says "transition WRITE_OFF" and R-15 "selects C-07 variant" | `set_field` outcomes (`assessment.write_off_indicated`; `settlement.c07_variant` via `value_from`+`value_map`); outcome execution + FSM guard wiring land in PRD-02 slice 2 (with #24) | CTO, PACKET-06 |
| 53 | Money percentage/rounding arithmetic unstated (C-01 2.5%; R-05 0.5×) | C-01 via Decimal `ROUND_HALF_EVEN` to integer cents (rounding mode needs capture confirmation); R-05 compiled integer-exact (`quote×2 > min(pav, si)`); date inputs bind as UTC epoch days | CTO, PACKET-06 |
| 54 | C-03 line `parent_reserve_id` referent undefined (no reserve table exists) | `parent_reserve_id` = most recent C-02 `calc_runs.id` for the claim; none → `blocked_on_inputs` | CTO, PACKET-06 |
| 55 | §2.6 golden tests "from corpus cases" need the corpus (item 7) + PRD-03 harness | Synthetic boundary goldens now (estimate=excess; 50.0% PAV; 4M; 50,000/50,001); corpus goldens become PRD-03 work — mirror of #36 | CTO, PACKET-06 |
| 56 | "Missing/**unverified** required input" — verification floor unstated; R-02 `context_fields` reference unregistered `client.*` paths | Default: any committed verification state qualifies, per-input `min_verification` override slot; `context_fields` never bound as inputs, unresolvable ones load with visible `pending_field_registration` marker | CTO, PACKET-06 |
| 57 | PRD-02 §2.2 names `json-logic-py`, which is the upstream project's GitHub name — no such PyPI distribution exists; the `json-logic` dist is the unmaintained 0.6.3 original without `min`/`max` | Dependency = `json-logic-qubit` (import `json_logic`): maintained fork, has `min`/`max`, preserves integers in numeric coercion (verified against R-05/money semantics) | CTO + builder flag, PACKET-06 |
| 58 | C-05 "per-supplier-line deltas" (PRD-02 §2.3) has undefined operands — supplier lines carry `{payee_party_id, amount}` only; no per-supplier estimate exists to delta against | Aggregate savings executes when no supplier lines present; with supplier lines, C-05 records `blocked_on_inputs` (`assessment.supplier_lines.per_supplier_delta_inputs`); no operands invented — unblocked when the per-supplier estimate inputs are captured | Builder flag + CTO, PACKET-06 |
| 59 | Rule/calc input binding needs claim fields, but `hydrate_claim` is the only public read and decrypts **every** PII field (each decrypt ledgered); R-12 additionally re-hydrates inside `routing_amount` — one evaluation on a PII-bearing claim writes 2 needless `pii.decrypt` rows | Accepted interim (decrypts are access-logged per ED-6a); PRD-02 slice 2 prerequisite: selective-hydration read path in `claim_core` (CTO-gated change) + single hydration per evaluation, so rule evaluation never touches PII paths it does not bind | CTO review of PACKET-06 build |
| 60 | PRD-02 slice 2 needs claim_core surface that PRD-00 packets never shipped | Three authorized changes, exactly: selective hydration (`hydrate_claim(..., paths=)`), FSM guard-hook registry (`register_guard_hook`), `app.state.clock`; anything further requires a new register entry | CTO, PACKET-07 |
| 61 | All motor template bodies + required-field lists (T-02's 7 merge fields, T-03's 16 fields, T-01 body prose) uncaptured (open items 5/6; T-01 structure = PRD-08 §8.4) | Every motor registry entry ships `pending_capture` with `blocked_on`; engine mechanics proven via fixture packs; T-01 body lands with the PRD-08 packet; field lists never invented | CTO, PACKET-07 |
| 62 | PRD-02 §2.4 names a `pdf` channel but PDF production policy (network-disabled Chromium, byte-stable regeneration) belongs to PRD-08 | `pdf` channel stores the rendered text artifact + `template.rendered`; binary PDF generation lands with PRD-08 | CTO, PACKET-07 |
| 63 | Outcome-verb execution details unstated: route→review-type mapping, decline draft item type, emit_event semantics (event catalog closed), route_approval consumer absent | `review_routes` map is pack config (`EX_GRATIA_REVIEW→EX_GRATIA`); `propose_decline`⇒`DRAFT_RELEASE{decline_draft}` with human-only release; `emit_event` executes the draft-template attempt only (pending template ⇒ visible `EXCEPTION{template_pending_capture}`); `route_approval` raises until PRD-08 wires approval flow | CTO, PACKET-07 |
| 64 | SURRENDER_CHECKLIST→SETTLEMENT guard references R-14, whose inputs are uncaptured (#49) | Edge conservatively hard-blocked (blocked rule never passes a gate); consistent with write-off claims stopping at SALVAGE_BIDDING until C-07 lands (guide §6) | CTO, PACKET-07 |
| 65 | PRD-00 defines `source_type='rule'` but no PRD states the verification state or provenance shape for rule-written fields | `set_field` writes: `source_type="rule"`, `verification_state="system_confirmed"`, `source_ref={rule_id, rule_version, rule_run_id}` — reconstructable to the exact run | CTO, PACKET-07 |
| 66 | PRD-03 package naming and module boundary | `platform/eval_harness/`, import `eval_harness`; models on `claim_core.Base`; access via root exports + `app.state` (ED-1) | CTO, PACKET-08 |
| 67 | G-CITE/G-NOTE need model calls; G-COMM has no producer before PRD-06; G-PROC needs `cop_steps.yaml` (PRD-13) + agent runs (Phase 2) | All four registered `pending` with `blocked_on`, visible in the grader registry; implementations land in PRD-03 slice 2 / their producing PRDs | CTO, PACKET-08 |
| 68 | AR-2 `execute_or_stage` gate is Section 0.5 machinery with no PRD-03 home; §3.3 "blocks at L3+" has no caller until agents exist | Gate module ships with the Phase-2 kickoff packet; PACKET-08 exposes the `grade_output` gating seam it will call | CTO, PACKET-08 |
| 69 | Promotion sign-off roles cannot be verified before PRD-04 RBAC | Sign-off records store actor + claimed role verbatim; L3→L4 requires two distinct actors; console-backed role verification lands with PRD-04 | CTO, PACKET-08 |
| 70 | §3.4 "all changes ledgered" but the ledger action map (PACKET-03) lacks autonomy events | Authorized claim_core change: map gains `autonomy.promoted`/`autonomy.demoted` (the map was designed to grow) | CTO, PACKET-08 |
| 71 | No PRD enumerates the capability id list or the "money-adjacent" set | Seed list assembled from doc-named ids (PACKET-08 §3.3); money-adjacent = settlement.*, repair.authorize/release, icon.reserve_adjust/assessor_payment_request/payment_voucher, salvage.* (max L3); `settlement.eft_match` provisional pending PRD-12; icon.*/settlement.* start L0; capture confirmation wanted | CTO, PACKET-08 |
| 72 | Promotion counters need `review.resolved` payloads before PRD-04 defines the four-part contract | Interim v0 payload schema {capability_id, resolution, diff{typed_changes[], prose_change_ratio}} defined in PACKET-08 §3.3; PRD-04's versioned resolution schemas supersede it | CTO, PACKET-08 |
| 73 | Protected Packet-01/03 acceptance fixtures predate the Packet-05 shared provenance ratchet and originally submitted generic uncited `extracted` writes | Enforce the higher-precedence no-provenance-no-commit invariant and revise those synthetic fixtures to create real same-claim citation documents; protected fixture changes require owner review | Codex, PACKET-05 build |
| 74 | ED-4a requires the previous pinned version of each tier as fallback but does not identify it; Anthropic's current lifecycle list has no active predecessor for Haiku 4.5 | Pack config exposes fail-closed `fallback_model_id: pending_capture`; primary live calls and official list-price metering are configured, but fallback switching remains blocked until CTO selects supported ids | Codex, PACKET-05 build |
| 75 | CC-1 requires registration observations from repair estimates and assessor reports, but PRD-01's closed extraction field lists contain no `reg` field for either document type | The configured check writes `insufficient` and `CONSISTENCY_FLAG` when a complete four-registration observation set cannot be formed; no current canonical registration is substituted for document-scoped evidence | Codex, PACKET-05 build |
| 76 | PACKET-03 register #32 allowed inline `pii.decrypt` ledger appends, while the Packet-05 CTO review clarifies that operational audit facts must reach the concurrency-one ledger writer through the event/outbox spine | `model.called` and `pii.decrypted` are durable events first; `LedgerWriter.consume` is now the only caller of the private append primitive, superseding #32 D-13 | CTO review remediation, PACKET-05 |
| 77 | Packet-04's locally designed `document_stages.status` enum has no ownership or provider-pause state, but PRD-01 requires resumable Celery stages and ED-4a requires exhausted provider runs to pause for explicit recovery | Extend the local enum with `running` and `paused`; only `pending` can be acquired, and `system`/`user:<ULID>` recovery moves `running|failed|paused` to `pending`. Downgrade maps live `running|paused` rows to visible `failed` rows requiring manual recovery before restoring the Packet-04 constraint | CTO review remediation, PACKET-05 |
| 78 | PRD-03 §3.4 defines L1→L2/L2→L3/L3→L4 promotion criteria but none for L0→L1 (shadow exit) | L0 capabilities fail closed: any L0→L1 promotion returns `CRITERIA_NOT_MET` until the shadow-exit policy is captured — never invented | Codex flag + CTO, PACKET-08 |
| 79 | §3.6 headline series (autonomy rate, no-touch rate, accuracy, median review time) have no defined denominators or windows | Series compute over committed all-time data; empty datasets return zeros; window/denominator definitions to be captured with the PRD-04 dashboard work | Codex flag + CTO, PACKET-08 |
| 80 | PRD-03 §3.5 says `approve_unchanged`, but interim review payload #72 says `approved|edited|rejected`; the payload also carries correction paths/kinds but not duplicate corrected values | Treat `approved` as unchanged/no capture; `edited|rejected` capture current append-only values only for named typed-change paths after the human write. Missing/unknown evidence creates a test case with `expected._capture.status=blocked_on_inputs`; never invent a label | CTO, PACKET-09 |
| 81 | PRD-03 names G-CITE/G-NOTE model logic but not their subject refs or structured model response schemas | Pin the minimal PACKET-09 §3.2/§3.3 contracts; calls use `doc_intel.llm.ModelWrapper`, prompts/rubrics/model ids/budgets are pack data, and money/date equality is independently checked in code | CTO, PACKET-09 |
| 82 | The seed-corpus `input_bundle` replay manifest and case→capability mapping are absent with master item 7 | Ship a `CorpusExecutor` boundary; synthetic executors prove full runner/scorecard mechanics. Unsupported real bundles are visibly `blocked_on_inputs` and never scored as passes until the corpus manifest is captured | CTO, PACKET-09 |
| 83 | PRD-03 §3.7's <30 min/100 cases gate and PRD-01 §1.7's 97/98/95/99 accuracy gates require the absent ≥100-claim corpus (master item 7; prior #36/#55) | Runner is proven on synthetic fixture corpora; timing and accuracy remain explicit live-ops gates when the real corpus lands. Synthetic results do not discharge them | CTO, PACKET-09 |
| 84 | PRD-01 §1.5 calls for monthly calibration, but the calibration inputs, window, reliability-curve output schema and threshold-change authority are unstated | No monthly calibration job in PACKET-09; thresholds remain unchanged pack data. Build no guessed statistics or automatic threshold mutation | CTO, PACKET-09 |
| 85 | PRD-03 §3.5 requires consistent pseudonyms but does not define anonymisation keys, mapping retention, binary-document handling or free-text detection | Claim-scoped HMAC pseudonyms from a runtime secret; mapping is process-memory only and never persisted; integer money is copied exactly; unclassified PII, free text and binary/image inputs refuse the whole export | CTO, PACKET-09 |
| 86 | PRD-03 requires weekly batch eval but states no wall-clock day/time | Pack-configured seven-day Beat interval from scheduler start; named task calls the synchronous runner. Wall-clock scheduling remains a replaceable config slot when operations supplies it | CTO, PACKET-09 |
| 87 | PACKET-09 corpus batch grades share `grader_runs` and `grader.failed` events with production, but PRD-03 §3.4 defines autonomy evidence and demotion triggers over production outcomes only | Grader runs with `test_case_id` are excluded from every promotion/demotion evidence window and from `evidence()`; the autonomy consumer ignores `grader.failed` events whose subject_ref carries `test_case_id`. Corpus results feed batch scorecards and the dashboard only | CTO review remediation, PACKET-09 |
| 88 | Register #85 covered classified scalar PII, but `claim_fields.value_type` permits `object`, leaving unclassified nested PII exportable through the anonymiser | Default-deny: only scalar value_types (`string`,`money`,`date`,`datetime`,`bool`,`enum`) with scalar values are exportable; free-text key detection recurses the whole bundle including `fields`; any other shape refuses the entire export | CTO review remediation, PACKET-09 |
| 89 | PRD-04 defines review-queue behaviour but no `review_items` DDL | Minimal event-sourced projection DDL from PACKET-10; rebuildable idempotently from `review.created`, and later column additions require a register entry | CTO, PACKET-10 |
| 90 | PRD-04 requires a workspace layout in every four-part contract but does not define component bindings before the console SPA | Store stable layout ids as pack data; PACKET-11 binds ids to React components | CTO, PACKET-10 |
| 91 | Reject requires enum + free text, but the reject-reason enum values are uncaptured | Free-text reason is mandatory; enum list remains `pending_capture` and membership is not guessed | CTO, PACKET-10 |
| 92 | Entra SSO belongs to PACKET-11, while PACKET-10 server-side authorisation needs trusted roles now | Mandatory `X-Actor` plus org-controlled `roles.yaml`; unmapped users, agents and system cannot resolve; SSO replaces transport in PACKET-11 | CTO, PACKET-10 |
| 93 | PRD-04 versioned resolution payloads supersede PACKET-08's interim v0 correction shape | Per-type `@1` schemas preserve the capability/diff correction core; breaking changes require a version bump and consumer migration | CTO, PACKET-10 |
| 94 | The ledger action map has no entries for review resolutions or authorisation denials | Add `review.resolved` and `authz.denied` mappings; the existing single-writer event consumer remains the only ledger writer | CTO, PACKET-10 |
| 95 | Post-triage decline approval (#23) has no transport that can commit the blocked FSM transition | Only a claims-manager-authorised EXCEPTION approval supplies `approved_by_event`; rejection leaves state unchanged and resolved projection state clears the blocked reason | CTO, PACKET-10 |
| 96 | Protected PACKET-10 FIELD_VERIFY acceptance fixture cited `01HREVQDOCUMENT0000000AAAA` without creating that document, conflicting with PRD-01's binding no-provenance-no-commit invariant (same class as #73) | Keep `422 CITATION_REQUIRED`; exercise FIELD_VERIFY with a real same-claim citation document. The owner corrected the protected fixture in `53725bb`; provenance validation remains unchanged | Codex + CTO, PACKET-10 |
| 97 | Review resolution and the existing public decline FSM method use separate transactions; a claim-state race can make the decline fail after `review.resolved` commits | Pre-validate the current claim state; on any decline commit failure, reopen the item and clear its resolution fields in a compensating transaction, then return `409 RESOLUTION_BLOCKED_ON_INPUTS`. The immutable resolution event remains as failed-attempt evidence; the open row keeps the work visible and retryable | CTO review remediation, PACKET-10 |
| 98 | FIELD_VERIFY and DOC_SPLIT side effects use existing public engine methods with their own transactions, so they cannot share the review row's transaction without a new claim_core/doc_intel surface | Re-check the locked-open status immediately before the side effect and again in the resolution transaction. A cross-worker race can still append a harmless duplicate human version before the losing resolver receives `409`; do not add a fourth claim_core change in this packet | CTO review note, PACKET-10 |
| 99 | ED-6 requires Entra SSO but does not define which token claim maps to the platform's ULID actor ids | Map exact immutable `(tid, oid)` pairs through deployment identity config to `user:<ULID>`; resolve role independently from org config; email, display name, groups and token roles grant nothing | CTO, PACKET-11 |
| 100 | Register #20 says Entra replaces `X-Actor`, while prior packet tests and in-process agent APIs still use that interim transport | Once the console ingress is installed, reject network `X-Actor` and inject only the verified mapped actor behind the compatibility membrane; uninstalled packet test apps remain unchanged, and machine-ingress auth waits for infra | CTO, PACKET-11 |
| 101 | PRD-04 S-2 requires checklist, projection-system and threaded-communication detail before their PRD-05/06/09 producers exist | Claim 360 aggregates only committed fields, documents and events; each unavailable section renders a visible owner-PRD marker, never fabricated rows or a silent blank | CTO, PACKET-11 |
| 102 | PRD-04 says every field opens a citation, but a human-verified correction's current `source_ref` points to the review rather than the extraction evidence | Resolve the newest valid same-claim extraction citation by walking append-only field versions; show the current value separately; absent/invalid evidence is `409 CITATION_UNAVAILABLE`, never rematched or invented | CTO, PACKET-11 |
| 103 | PRD-04 does not pin the Claim-360 header's insured, registration or amount field paths | Bind `parties.insured.name`, `vehicle.reg`, and routing amount `settlement.payable` with `reserve.total` fallback, following the existing dictionary/PRD-02 routing contract; missing values stay null | CTO, PACKET-11 |
| 104 | PRD-04 acceptance scenarios combine automatable mechanics with human timing, sampled visual judgement and production latency | Protected tests cover keyboard mechanics and 50 exact bbox transforms; the 20-item human median, 50 sampled visual misses and p95/page timing remain explicit live trial gates and cannot be discharged by synthetic CI | CTO, PACKET-11 |
| 105 | PRD-04 names role purposes but does not define S-1/S-2 route rows for every role | S-1/S-2 permit operational claims roles, HOC and read-only auditor; finance/admin fail closed until their named payment-adjacent/S-6 surfaces. Per-type resolution roles and bands remain narrower and authoritative | CTO, PACKET-11 |
| 106 | PRD-01 FIELD_VERIFY events predate PACKET-10's mandatory capability attribution and do not carry a browser value contract; PII candidates are event-redacted | Pin the existing PACKET-10 attribution label `doc.extract` as `doc_intel.yaml` pack data; emit dictionary-compatible `value_type`; hydrate redacted candidates only inside the authenticated review service and never expose `candidate_blob_ref` | CTO review remediation, PACKET-11 |
| 107 | A below-threshold FIELD_VERIFY candidate has no current claim-field version, while PACKET-10's resolver only permits corrections to existing fields and gives approve no side effect | Approve or Edit→Approve writes exactly the reviewed path as one append-only `human_verified` version, creating version 1 when absent. Preserve an exact same-claim review citation in the human source_ref; never insert a placeholder extracted version | CTO review remediation, PACKET-11 |
| 108 | PRD-04 requires email notifications but the shared mailbox and service accounts remain uncaptured under open item 1 | In-app notifications are live; every email row commits as `staged` with `blocked_on: open-item-1`, never dropped or reported sent | CTO, PACKET-12 |
| 109 | Notification sends and SLA escalations require ledger coverage but the existing action map has no entries | Add `notify.sent`, `notify.staged`, and `sla.escalated`; the concurrency-one ledger consumer remains the sole writer | CTO, PACKET-12 |
| 110 | No PRD supplies persistence DDL for staff notifications | Add one minimal `notifications` projection table; it is rebuildable apart from its permitted read-status mutation | CTO, PACKET-12 |
| 111 | PRD-04 names immediate notification events but not their staff audiences | Pack data provisionally maps SLA/projection events to the assigned officer and critical grader/demotion events to `claims_manager`; Mayfair confirmation remains required | CTO, PACKET-12 |
| 112 | PRD-04's “bulk escalate” has no defined mutation semantics | Emit one `sla.escalated` event and staff notification per clock; `pending_capture` roles return visible per-row `blocked_on_inputs`; claim and clock state never mutate | CTO, PACKET-12 |
| 113 | Digest contents and idempotency are unstated beyond 08:00 EAT and owned claims | Summarise committed open items, warned/breached clocks, and owned-claim states; deduplicate per officer and EAT date; schedule remains pack data | CTO, PACKET-12 |
| 114 | Register #79 dashboard trend windows and denominators remain uncaptured | Point-in-time and EAT-calendar tiles are live and CSV exportable; windowed trends stay `pending_capture` and export returns `409 SERIES_BLOCKED_ON_INPUTS`; production latency remains a live gate | CTO, PACKET-12 |
| 115 | S-6 requests user/role management but identities and roles are org config with no mutation schema | Ship a read-only config-provenance viewer; browser mutation remains deferred | CTO, PACKET-12 |
| 116 | S-3's PDF, drafted note, and T-03 body belong to unbuilt or uncaptured producers | Render explicit PRD-08 unavailable panes and a `pending_capture` T-03 slot; the KES 4M threshold is pack config | CTO, PACKET-12 |
| 117 | S-6 requires adapter health before PRD-09 defines an adapter registry | Render `unavailable{owner: PRD-09}` and invent no adapter rows | CTO, PACKET-12 |
| 118 | Browsers cannot attach an Authorization header to a websocket handshake | `/console/ops/ws` authenticates its first message with the installed token verifier and closes untrusted clients with 4401; every HTTP console route remains bearer protected | CTO, PACKET-12 |
| 119 | Phase-2 needs separate names for the shared runtime and the first agent package | Shared runtime is `platform/agent_runtime/` (import `agent_runtime`); intake is `agents/intake_agent/` (import `intake_agent`), mirroring #18/#33/#46/#66. Because all package models share `claim_core.Base`, Alembic imports `AgentRun` for complete target metadata while `initialise_database` excludes `agent_runs` until `build_agent_runtime` installs its owned table. | CTO, PACKET-13 |
| 120 | `email.received` has no Graph producer and organisation self-address capture remains under open item 1 | Router consumes durable events and supports configured `self_addresses`; transport remains `pending_capture` and cannot enable sending while addresses are uncaptured | CTO, PACKET-13 |
| 121 | Confident `new_intimation` routing targets the S1–S8 flow, which belongs to PACKET-14 | Emit durable `intake.requested`; create no claim in this slice | CTO, PACKET-13 |
| 122 | PRD-05's “substantive new evidence” and SETTLED/CLOSED money relevance lack executable signals in this slice | Attachment-bearing DECLINED/WITHDRAWN inbound creates `REOPEN_PROMPT`; SETTLED/CLOSED attach and notify only until PACKET-14 supplies document-type signals | CTO, PACKET-13 |
| 123 | PRD-05 §5.6 supersedes the provisional capability seed for its seven rows | Add `intake.doc_request`; set the §5.6 initial/max levels exactly; retain #78's fail-closed L0→L1 rule and permanent L1 ex-gratia ceiling | CTO, PACKET-13 |
| 124 | T-06a is absent and the provisional T-06 verification floor conflicts with PRD-05 §5.5 | Register T-06a and set T-06a/T-06 to `min_verification: extracted`; both bodies remain `pending_capture` on item 6 | CTO, PACKET-13 |
| 125 | AR-1a requires an ops alert after three failed step attempts, but no paging/email transport exists before item 1 | Mark the run `failed` and create visible `EXCEPTION{agent_run_failed}` work; real alert transport remains item-1 scope | CTO, PACKET-13 |
| 126 | AR-2 does not map L2 capabilities to closed review-item confirm types | Pack `gate.yaml` owns the mapping; an unmapped capability fails closed to `DRAFT_RELEASE` | CTO, PACKET-13 |
| 127 | Graph archive-label writes do not exist before item 1, while auto-archived mail must remain queryable and sampled | Persist inbound communication + `mail.archived`, deterministically sample by message id at the pack rate, and count archived mail in digest data; Graph write remains blocked | CTO, PACKET-13 |
| 128 | Claim assignment lacks ledger mapping and a deterministic equal-weight tie-break | Add `claim.assigned` to `ACTION_MAP`; ties resolve by lexicographic actor id | CTO, PACKET-13 |
| 129 | G-COMM's deterministic checks become executable when AR-3 lands | Replace its pending grader with critical recipient/registry/verification checks; G-PROC remains pending | CTO, PACKET-13 |
| 130 | AR-3a defines a window but no durable release queue before the Graph transport packet | Compute and return visible `queued_window`; durable queue/release lands with item 1; `intake.acknowledge` stays pack-configured 24/7 exempt | CTO, PACKET-13 |
| 132 | PRD-00's per-claim SHA-256 document dedupe returns 409 when a distinct inbound message re-sends identical attachment bytes, while PRD-05 requires the mail to remain queryable and never silently dropped | Keep the original document row, keep the newly recorded inbound communication, and append `INBOUND_DUPLICATE_ATTACHMENT` to the claim timeline with the message id, attachment index and digest; create neither a duplicate document nor an exception | CTO review, PACKET-13 |
| 133 | AR-3a makes `packs/motor/sla/holidays.yaml` authoritative for send windows, but the identical fixed-date calendar remains at `platform/claim_core/sla/holidays.yaml` for the existing SLA engine | Keep both content-identical in PACKET-13; consolidate the SLA engine onto the pack-owned calendar in a later coherent packet | CTO review, PACKET-13 |
| 134 | AR-1 `expects_events` declared no executable wait outcome | A step may persist `waiting{expects_event}` without consuming an attempt; matching durable events re-invoke it and the reaper remains the backstop | CTO, PACKET-14 |
| 135 | S1 claim creation is governed at launch L2, but `intake.claim_creation` has no L2 confirm mapping and §5.6 calls creation reversible | Route `intake.create_claim` through `execute_or_stage`; the unmapped L2 capability fails closed to `DRAFT_RELEASE`, and approval executes then resumes the COP run | CTO, PACKET-14 |
| 136 | FRAUD_SIGNAL is absent from PRD-04's closed 17-type review enum | Emit durable `fraud.signal`, ledger it, and surface its payload on the coverage card; approval-note rendering remains PRD-08 | CTO, PACKET-14 |
| 137 | S4 does not define duplicate-window inclusivity, terminal-state membership, or missing-input behaviour | Use ±3 days inclusive; terminal set `SETTLED/CLOSED/DECLINED/WITHDRAWN/VOID` within 365 days inclusive; missing reg/date records `insufficient` and continues | CTO, PACKET-14 |
| 138 | R-10 is blocked on uncaptured `pack.late_days`, but §5.3 does not say whether that pauses the COP run | Persist the `blocked_on_inputs` rule run and continue; unverified/missing loss date has the same visible blocked semantics | CTO, PACKET-14 |
| 139 | Scenario 3 says “no acknowledgement-of-processing” although T-06a precedes triage | Auto-release nothing on the decline path; the earlier receipt acknowledgement remains a human decision and is never silently withdrawn | CTO, PACKET-14 |
| 140 | PRD-05 says decline subtype `decline`, while register #63 and protected PACKET-07 assert `decline_draft` | Preserve `decline_draft`; reconcile the label in the next spec round | CTO, PACKET-14 |
| 141 | Decline-draft resolution had no T-07 release wiring | Approve requires TRIAGED and pre-validates T-07; while it is `pending_capture`, return `409 RESOLUTION_BLOCKED_ON_INPUTS` and keep the item open; once captured, commit `decline(below_excess)` and govern/stage the letter through AR-3 under `triage.decline_draft` | CTO, PACKET-14 |
| 142 | Mode A's coverage card had no subtype contract and §5.4's “five keys” counts the period pair ambiguously | Add `coverage_manual` / `FIELD_VERIFY_COVERAGE@1`, register the six named paths, and commit supplied nonblank values as one append-only human batch; excess-protector semantics remain uncaptured | CTO, PACKET-14 |
| 143 | R-02 context names unregistered `client.loss_ratio` and `client.premium_history` fields | Keep them unregistered and show `pending_field_registration` markers on the decline draft | CTO, PACKET-14 |
| 144 | §5.3 names `intimation.date`, which is not a registered dictionary path | Write the source email timestamp to registered `intimation.received_at` as a system-sourced, confidence-null value | CTO, PACKET-14 |
| 145 | The acknowledge clock lacked a stop event and the KPI had no live series | Stop only on `email.sent{template_id:T-06a}`; expose live count/median over stopped clocks with honest zeros before transport exists | CTO, PACKET-14 |
| 146 | Scenario wall-clock limits require a real inbox and trial environment | CI proves mechanics only; measure <5 minute and <15 minute inbox gates in the live trial | CTO, PACKET-14 |
| 147 | S7's PRD-06 hand-off had no durable event-catalog contract | Emit idempotent `chase.init` with pack-owned base item ids and fail closed to include police abstract while R-11 is blocked | CTO, PACKET-14 |
| 148 | SETTLED/CLOSED money-relevant document types are absent from the motor taxonomy | Ship the `document.classified` consumer and `money_relevant_doc_types: pending_capture`; never guess invoice/demand types | CTO, PACKET-14 |
| 149 | R-02 needs `assessment.estimate_total`, but the normal intake sequence reaches coverage triage before PRD-06 obtains an estimate | Preserve the `blocked_on_inputs` `rule_runs` row, complete S8, and transition `INTIMATED→TRIAGED`; do not invent an exception or make the later estimate a triage-completion precondition | CTO review, PACKET-14 |
| 150 | Reject semantics for the L2 staged `intake.create_claim` action were not defined | A rejected creation confirm completes the durable run as a visible no-op, marks remaining steps skipped, creates no claim, and never enters reaper retry/failure handling | CTO review, PACKET-14 |
| 151 | S6 assumes the sender party created by S3, but no recovery contract existed if that invariant is broken | Record the acknowledge step as `insufficient{intimation_sender_party}` and continue; create no new EXCEPTION subtype. Operators repair the upstream party invariant from the durable step outcome | CTO review, PACKET-14 |
| 152 | Rejecting the Mode-A coverage card had no re-key path and stranded the claim in `INTIMATED` | Idempotently issue a fresh `coverage_manual` card linked by `retry_of`; keep the run awaiting review and create no locally invented exception | CTO review, PACKET-14 |
| 153 | PACKET-14's pinned bare `/portfolio` route duplicates the existing role-equivalent `/console/ops/portfolio` read surface | Keep both routes for packet compatibility; consolidate onto the console namespace in a later API-version packet, preserving the same RBAC and response contract until removal is announced | CTO review, PACKET-14 |
| 154 | A workflow-owned AR-2 action can reuse its parent `agent_runs` id, so its per-action autonomy level is not snapshotted independently (AR-1 auditability gap) | Retain action/capability/review attribution on step outcomes and events; standalone governed actions keep their own run snapshot. Add per-action level evidence in a coherent runtime schema packet, with migration/register review | CTO review, PACKET-14 |
| 155 | PRD-05 fixes checklist item ids but not their document-taxonomy matching map | Store the item→accepted-doc-types map in `packs/motor/intake/intake.yaml`, validate exact coverage at load, and keep matching code data-driven | CTO review, PACKET-14 |
| 156 | S4 duplicate matching has no indexed claim-core query at the 50-intimations/day launch envelope | Retain the deterministic whole-book scan for launch correctness; replace it with an indexed current-field/date query before corpus or production volume exceeds the trial envelope | CTO review, PACKET-14 |
| 157 | Approval of an AR-3 staged communication cannot execute a send while the item-1 transport/release executor is absent | Resolve the human decision without fabricating `email.sent`; release-executes-send lands with the item-1 transport packet, mirroring #130's durable release-queue boundary | CTO review, PACKET-14 |
| 158 | PRD-06 agent package naming and shared-model boundary were unstated | Use `agents/chase_agent/`, import `chase_agent`; tables share `claim_core.Base` and install only through `build_chase_agent` | CTO, PACKET-15 |
| 159 | The routed new-intimation communication initially has no claim id, breaking later thread matching | Add one claim-core method that links that exact Graph message to the S1-created claim; intake calls it immediately after governed creation | CTO, PACKET-15 |
| 160 | PRD-06 names no owner for the initial T-06 request or cadence anchor | Govern it under `intake.doc_request`; a staged outcome sets `requested_at` and anchors cadence | CTO, PACKET-15 |
| 161 | PRD-06 §6.4 names `snooze_until`, absent from §6.2 DDL | Add the nullable timestamp to `chase_items`; snoozed items are excluded until the timestamp passes | CTO, PACKET-15 |
| 162 | “Passing validators” and the four reject reasons lack a producing-signal contract | Verify no-target schemas on classification; otherwise require a committed field; zero commits means `illegible`, document-linked CC-1 mismatch means `wrong_vehicle`; never invent expired/wrong-document signals | CTO, PACKET-15 |
| 163 | The defect-specific re-request template id is unnamed | Use the registered T-06r tone variant with structured defect data; pending bodies remain visible staged drafts | CTO, PACKET-15 |
| 164 | `doc_item_age` needs independent clocks for seven same-claim items | Add optional definition `key_field`; stop only clocks whose start-event key matches the stop-event key | CTO, PACKET-15 |
| 165 | PACKET-14's intake doc-type map duplicates PRD-06's authoritative item registry | Remove the intake map and make S7 read `checklists/items.yaml` | CTO, PACKET-15 |
| 166 | The condition for auto-adding `cert_of_incorporation` is uncaptured, and its `doc_type` is absent from the closed PRD-01 motor registry | Register the dormant checklist item but never auto-instantiate or match it until a document-schema spec round registers both the condition and doc type | CTO, PACKET-15 |
| 167 | Logbook bank-interest has no registered extraction target | Register `logbook.bank_interest.present` for human input only; do not alter the closed document schema | CTO, PACKET-15 |
| 168 | AR-3 has no cc-recipient channel for reminder 2 onward | Add the insured to `to_party_ids`; true cc semantics remain item-1 transport scope | CTO, PACKET-15 |
| 169 | Chase reminder and re-request autonomy ceilings are unstated | Launch both at L1 with L4 max and use the normal stepwise promotion ladder | CTO, PACKET-15 |
| 170 | PRD-06 chase state events lacked audit action mappings | Add the requested/received/verified/rejected/waived/snoozed/reminder/complete/cancelled mappings; ledger remains single-writer | CTO, PACKET-15 |
| 171 | PostgreSQL materialised views cannot be exercised before the PG infra packet | Ship dialect-portable computed readers with the pinned data contracts; materialisation/refresh remains infra scope | CTO, PACKET-15 |
| 172 | “Inbound reply within 24h” lacks a durable signal definition | Use the newest inbound communication linked to the claim; for every outstanding item set `next_reminder_at = max(current, inbound + 48h)`, so the whole checklist is deferred without pulling later reminders earlier | CTO, PACKET-15 |
| 173 | Surrender checklist completion was not connected to the existing R-13 field guard | Instantiate on entry to `SURRENDER_CHECKLIST`; physical attestations append human `salvage.*_held=true` fields; R-14 remains blocked on #49 | CTO, PACKET-15 |
| 174 | Once `agents/chase_agent` exists, Ruff classifies `chase_agent` as first-party and flags the protected PACKET-15 fixture's pre-existing `chase_agent`-before-`fastapi` local import order | Fixture owner reordered the protected local import block; builder preserves that owner-owned hunk and repo-wide Ruff remains green | Builder flag + CTO fix, PACKET-15 |
| 175 | AR-3's existing `send` interface has no channel for the structured outstanding/received/defect context required in PACKET-15's pinned staged-action payloads | Add an optional action-payload mapping that is merged only after recipient/attachment validation and cannot override any AR-3-owned transport, template, rendering, or signing field | Builder flag, PACKET-15 |
| 176 | PACKET-14 always assigns the intimation sender the `broker` role, so client-intimated sender-role fidelity and tone selection are not captured | Select the requester only by `parties.meta.source=intimation_sender`; use its stored role for tone and fail visibly if the marker is absent/ambiguous. Capture truthful sender roles in a later intake spec round | CTO review, PACKET-15 |
| 177 | PRD-06 requires a re-request for each rejected replacement document but does not define its idempotency key | Deduplicate on `(item_id, source_document_id)` rather than item lifetime; redelivery of one rejection is a no-op, while a second bad replacement creates a new staged re-request | CTO review, PACKET-15 |
| 178 | `kra_pin` and `kra_pin_cert` share doc type `kra_pin_cert`; if claim-doc and surrender checklists overlap, an inbound certificate has two exact candidates | Preserve never-guess behavior with `EXCEPTION{chase_match_ambiguous}`. Officer resolves one requirement independently (or CM waives a justified blocking item); a later distinct document event can match the sole remaining item. Capture a manual match/replay contract before automating reuse | CTO review, PACKET-15 |
| 179 | A claim can complete both claim-doc and surrender checklists, so a claim-id-keyed chase-cycle map overwrites one duration | Key the live series by checklist id and include claim id in each row; aggregation across purposes remains a future dashboard-window decision | CTO review, PACKET-15 |
| 180 | At L3+, pending Graph transport refuses each governed reminder attempt; a due item is therefore reconsidered every 15-minute tick and can create noisy blocked action runs | Keep retryable/fail-closed behavior until item 1 supplies transport, but document the noise; operators should treat repeated `transport_pending_capture` runs as one dependency outage, not separate claim defects | CTO review, PACKET-15 |
| 181 | PRD-07 assessment-agent package name and model ownership were unstated | Use `agents/assessment_agent/`, import `assessment_agent`; vendor models share `claim_core.Base` and install through `build_assessment_agent`; migration `0012_vendors_assessment` | CTO, PACKET-16 |
| 182 | The estimate-verified trigger, first FSM-hop owner, and repeat-estimate semantics were unstated; the original state enumeration omitted the expected estimate-at-intimation case | Consume committed `assessment.estimate_total`; while `INTIMATED`, defer silently and replay the held committed estimate on `claim.status_changed→TRIAGED`; then step `TRIAGED→AWAITING_DOCS→IN_ASSESSMENT`; suppress terminal claims, raise `assessment_out_of_sequence` only for genuinely unexpected live states, and keep one open mode card per claim | CTO amendment, PACKET-16 |
| 183 | `MODE_CONFIRM@1` contains no mode or vendor decision | Add `MODE_CONFIRM@2` with `decision{mode,vendor_ids}` and retain @1 for historical replay | CTO, PACKET-16 |
| 184 | PRD-07 names assessment mode fields absent from the pack dictionary | Register `assessment.mode` and `assessment.multi_mode`; commit the officer resolution as append-only `human_verified` evidence | CTO, PACKET-16 |
| 185 | PRD-07 permits mode-confirm L3 while the binding guide caps it at L2 until R-06 capture | Launch `assessment.mode_confirm` at L2 so AR-2 creates the typed confirm; preserve max L2; any increase requires Q-02, 50 clean confirms, a pack change, and owner sign-off | CTO, PACKET-16 |
| 186 | Real assessor firms and per-firm fee schedules have not been captured | Ship the three standard fees as cents in pack data, keep production vendor seeds empty/pending, and inject synthetic firms only in tests. The pack is source of truth: startup upserts every captured row, including `active`, so an operational deactivation must be committed to pack data rather than made only in the database | CTO, PACKET-16 |
| 187 | AR-3 addresses claim parties rather than vendor registry rows | Idempotently bridge each selected firm to one claim-scoped `assessor` party using its captured first email and `meta.vendor_id` | CTO, PACKET-16 |
| 188 | PRD-07 does not name a separate vendor-selection surface | Carry vendor picks in `MODE_CONFIRM@2`; each selected firm then receives its own governed L1 dispatch draft | CTO, PACKET-16 |
| 189 | No observed T-11 email exists in the repository for the proposed interim reconstruction | Keep T-11 `pending_capture`, expose structured merge/attachment gaps, and invent no prose | CTO, PACKET-16 |
| 190 | Assessor-report reminder reuse needs an explicit requester and tone | Widen checklist purpose, add nullable `requester_party_id`, register T-06r-assessor pending capture, and reuse the existing cadence/suppression engine; assessor-report reminders remain assessor-and-broker workflow traffic and do not add the insured-recipient escalation used by document chase | CTO, PACKET-16 |
| 191 | The assessor turnaround clock lacked executable start/stop and multi-firm keying | Start on staged `assessment.dispatched`, stop on PACKET-17's `assessment.report_received`, key by assessor party, and retain business 3d/5d thresholds | CTO, PACKET-16 |
| 192 | Shadow mode names vehicle age without a registered field and must never promote or surface rationale | Pass vehicle age as null, cap `assessment.mode_shadow` permanently at L0, and make one isolated shadow attempt per newly issued mode card (including reissues); a Path-B model or logging failure never unwinds or suppresses Path A. Keep rationale redacted and all shadow output off review, event, and claim-read surfaces | CTO amendment, PACKET-16 |
| 193 | Weekly Path-B agreement needs the unavailable ≥100-claim corpus | Persist each card-linked shadow attempt in its L0 agent run: successful runs retain `mode` and `confidence` with a redacted rationale marker; failed attempts retain only safe failure metadata. Pair those runs with labelled officer decisions later; defer the agreement gate to the PRD-03 corpus measurement | CTO amendment, PACKET-16 |
| 194 | PRD-07 assessment events lacked an explicit event-catalog and ledger mapping | Register/ledger mode-item, mode-decision, and dispatch events; reserve `assessment.report_received` as the PACKET-17 SLA stop event | CTO, PACKET-16 |
| 195 | PACKET-16's pinned shadow assertion reads `agent_runs.level`, while binding AR-1 names the stored column `autonomy_level` and PACKET-13 pins that exact DDL | Preserve the binding `autonomy_level` DDL with no runtime schema alias; the fixture owner corrects the protected assertion to read `autonomy_level` | CTO fixture correction, PACKET-16 |
| 196 | PRD-04 resolution schemas cannot validate selected vendors against the live package-owned registry | Add one fail-closed, package-registered resolution-validator hook; assessment uses it before resolution commits to return 422 for unknown/inactive vendors and 409 when its captured broker input is absent | CTO review, PACKET-16 |
| 197 | The PRD-04 contract loader previously fixed every review resolution schema at `@1`, while PACKET-16 requires `MODE_CONFIRM@2` and historical `@1` replay | Accept positive `TYPE@N` schema references from pack contracts and load that exact file; the closed 17-type enum remains unchanged | CTO review, PACKET-16 |
| 198 | AR-2's staged-action envelope omitted mechanics required by existing resolution consumers: communication co-recipients and review reissue linkage | Copy validated communication `to_party_ids` and string `retry_of` into the review envelope while preserving the canonical action payload; this changes no autonomy level, review-type selection, or executor behaviour | CTO review, PACKET-16 |
| 199 | Permanent-L0 assessment shadow runs need a privacy-safe durable output slot for weekly comparison, but the shared gate previously persisted only generic action status | Add optional caller-sanitised `Action.log_payload`, persisted only in an L0 run outcome; assessment stores card id, mode and confidence, replaces rationale with `__redacted__`, and records only error type on failed model attempts | Builder flag, PACKET-16 |
| 200 | PRD-07 cascade needs persisted extracted report data that is intentionally not committed to `claim_fields` | Keep the cascade in `agents/assessment_agent/{report,cascade,selection}.py`, add migration `0013_savings_ledger`, and expose only `doc_intel.extraction_output(document_id)` as the curated read of persisted EXTRACT output | CTO, PACKET-17 |
| 201 | T-11 transport is pending, so assessor replies cannot thread-match a real outbound conversation | Accept the PRD-05 reference/manual-triage claim link, then attribute only when the inbound sender equals one dispatched assessor-party email; otherwise raise `EXCEPTION{report_unattributed}` and do not cascade | CTO, PACKET-17 |
| 202 | PRD-07's report financial floor did not name its persistence check | Require the committed report version's confidence to be at least 0.90 or its state to be `human_verified`; re-arm from the later `field.updated` after review | CTO, PACKET-17 |
| 203 | The closed assessor-report schema exposes fees but has no fee target paths | Never auto-commit or substitute a standard fee; run C-02 visibly blocked until the officer keys assessor and reinspection fees, then re-attempt automatically | CTO, PACKET-17 |
| 204 | R-07's pack rule cannot compare a cross-report array, and override handling was unstated | Keep R-07 `blocked_on_inputs`; select deterministically by lowest quote then lowest party id, cite R-07 in `assessment.selection_completed`, and raise `EXCEPTION{selection_overridden}` after a differing human quote while preserving the human value | CTO, PACKET-17 |
| 205 | Multi-assessor wait-all and timeout resolution lacked an executable completion contract | Treat every dispatched party as required; on SLA breach issue one open `PROCEED_PARTIAL`, approve to select among verified received reports, reject to keep waiting, and never proceed with zero reports | CTO, PACKET-17 |
| 206 | Supplier-line savings operands exist only in extracted report data and supplier vendors are uncaptured | Normalise complete garage/supplier prices from shillings to cents, append evidence rows with null `vendor_id`, and list incomplete lines in header evidence without fabrication | CTO, PACKET-17 |
| 207 | PRD-07 names reserve output but no pack dictionary entry exists | Register `reserve.total` as non-PII money and append it from the executed C-02 run with calc provenance | CTO, PACKET-17 |
| 208 | `projection.requested` has no consumer before PRD-09 | Emit and ledger the fire-and-forget request after C-02; add no retry or acknowledgement semantics in PRD-07 | CTO, PACKET-17 |
| 209 | The interim savings dashboard summed C-05 runs, conflicting with §7.6 header-only billing | Re-point the unchanged MTD/YTD contract to `savings_ledger.kind='assessment_negotiation'` and ledger report, selection, projection, and savings events through the existing writer | CTO, PACKET-17 |
| 210 | The multi-assessor comparison artifact had no durable home | Store the ordered firm comparison in `assessment.selection_completed` and the partial-review payload; console rendering remains PRD-04 scope | CTO, PACKET-17 |
| 211 | Ownership of the cascade FSM hops was unstated | The assessment cascade owns `IN_ASSESSMENT→REPORT_RECEIVED` after verification/selection and `REPORT_RECEIVED→WRITE_OFF` only after R-05 fires; the ICON registration edge remains untouched | CTO, PACKET-17 |
| 212 | The protected PACKET-17 fixture built the assessor report as one PDF page per line while every extracted field cited page 1, so anchors on later pages failed citation | Fixture defect, owner-corrected: the report fixture is a single-page PDF so all page-1 anchors resolve; PRD-01 §1.4 citation mechanics are unchanged and the builder's compensating cross-page-recovery patch is reverted (binding invariant, not modified to fit a test) | CTO fixture correction, PACKET-17 |
| 213 | Alembic batch recreation of `chase_checklists` cannot preserve PostgreSQL dependants on its primary key | Use native PostgreSQL constraint/column ALTER operations; retain batch recreation only for SQLite, including symmetric downgrade handling | CTO review remediation, PACKET-17 |
| 214 | The PACKET-16 Path-B model was called before its L0 run and outside AR-4 governance | Declare a recoverable `assessment.mode_shadow` COP step, start its durable L0 run first, take tier/prompt/budgets from pack data, use `ModelWrapper`, ledger `model.called`, and route any budget breach to `EXCEPTION{budget_exceeded}` without damaging Path A | CTO review remediation, PACKET-17 |
| 215 | Supplier-line values live in EXTRACT output but §7.6 requires resolved citations on every billable ledger row | Add a narrow CITE-output read; write supplier decomposition rows only when their field has a resolved anchor/bbox citation, otherwise record the line as incomplete with `unresolved_citation` and fabricate no evidence | CTO review remediation, PACKET-17 |
| 216 | Multiple assessor-report revisions from one firm have no authoritative-version rule | Fail closed with `EXCEPTION{assessment_report_revision_ambiguous}` and create no selection until an authoritative-revision contract is captured; exact redelivery of the same party/document pair remains idempotent | Builder ED-11 flag + CTO review, PACKET-17 |
| 217 | A claim-level `projection.requested` existence check suppressed corrected reserve runs after fee updates | Deduplicate projections by the executed C-02 input mapping; corrected fee versions create a new C-02 run, append a new reserve version, and emit a new projection while identical inputs remain idempotent | CTO review remediation, PACKET-17 |
| 218 | The protected PostgreSQL boundary test created two app/blob-store environments against one shared database, so the second environment resolved stale stage rows to absent local artifacts | Fixture defect, owner-corrected: exact-boundary and one-shilling-over cases are separate test items, so the PostgreSQL isolation fixture resets durable state between their private blob stores. Production code does not recover across unrelated blob stores | CTO fixture correction, PACKET-17 |
| 219 | PRD-08's approval-pack package name, installer, and model ownership were unstated | Use `agents/approval_pack_agent/`, import `approval_pack_agent`, public `build_approval_pack_agent`; `note_drafts` shares `claim_core.Base` and installs through the builder; migration `0014_note_drafts` | CTO, PACKET-18 |
| 220 | Merged-pack versions have no PRD DDL table, and concurrent generation could collide | Index immutable versions by append-only `pack.merged` events; assign the next integer under the claim lock; add no table absent from the PRD DDL | CTO, PACKET-18 |
| 221 | PRD-08 §8.3 lists 13 pack items in prose with no machine-readable schema | Pin `packs/motor/approval_pack/manifest.yaml` to exactly `{id, order, label, source_kinds, selector, repeatable, conversion, required, waivable}` plus `doc_types` for `auto_doc_type`; startup rejects unknown keys, duplicate ids/orders, non-contiguous order, and unregistered document types | CTO, PACKET-18 |
| 222 | PRD-08 refuses on missing non-waived items but defines waiver authority and evidence for none of them | Ship all 13 rows `required: true, waivable: false`; never infer a waiver from a similarly named chase item. A later pack version may enable a captured waiver contract | CTO, PACKET-18 |
| 223 | Items 1/2/9/11 have no document taxonomy entry and communications carry no semantic-purpose column | Resolve them from explicit claim-owned `pack.sources_selected` events; never invent `policy_schedule`/`supplier_quote` doc types and never infer 'first' or 'nearest' email | CTO, PACKET-18 |
| 224 | Items 12/13 name a projection readback that PRD-09 does not yet emit | Provide a fail-visible resolver seam that reports `pending_integration`; officer PDF upload via `POST .../manifest/{item_id}/upload` makes both rows live now. Do not invent a PRD-09 event name or scrape ICON state | CTO, PACKET-18 |
| 225 | Readiness could change between the officer's read and the generation request | Make the canonical SHA-256 readiness fingerprint mandatory at generation; a changed input returns 409 with the recomputed card and no partial artifact | CTO, PACKET-18 |
| 226 | S3 Object Lock is not certified, but merge must not use a mutable put | Merge writes only through the `ImmutableArtifactStore` protocol; the local write-once adapter refuses a differing overwrite and reports `object_lock_status=local_write_once`. Production publishability stays blocked on #30/#116 | CTO, PACKET-18 |
| 227 | PRD-08 requires reproducible packs but names no normalisation contract | Fixed clock plus identical sources, selection, manifest and renderer must be byte-identical; converted sources are content-addressed and reused, PDF metadata and the document id are deterministic, and only the visible EAT timestamp header may differ otherwise | CTO, PACKET-18 |
| 228 | The six PRD-08 pack events and the existing `template.rendered` path had no audit action mappings | Register `pack.sources_selected`, `pack.item_uploaded`, `pack.generation_refused`, `pack.merged`, `pack.note_drafted`, and `template.rendered` in the single-writer ledger map; no package-local audit writer exists | CTO, PACKET-18 |
| 229 | Nine T-01 computed slots have no captured source, formula, or rounding contract | Register them as structured `blocked_on_inputs` and render a visible `BLOCKED ON INPUTS` marker. Only C-08's `amount_payable` uses the PRD-mandated `PENDING CAPTURE` text. Add no field, formula, or default false/zero; the draft root stays `signable: false` | CTO, PACKET-18 |
| 230 | The driver-is-insured party-match producer is uncaptured and other CC evidence may be absent | Render only persisted structured CC results; `driver_is_insured` is blocked at launch and any missing CC evidence renders blocked. A `CC-5 flagged` result is copied verbatim and can never be normalised to a pass | CTO, PACKET-18 |
| 231 | PRD-08 does not define what happens when generated commentary is unverifiable | Require the exact three-paragraph schema, numeric multiset equality between `numbers_used` and the text, and membership in the allowed-number multiset; the first failure triggers exactly one governed regeneration carrying only validation errors, the second creates `EXCEPTION{note_commentary_invalid}` with no note draft and no FSM hop. Never repair prose or delete a number in code | CTO, PACKET-18 |
| 232 | G-NOTE previously verified only claim fields across the whole artifact | Scope grading to the three rendered commentary nodes and independently reload the cited claim field, calc run, or immutable savings row; unsupported assertions, missing sections, liability tone, source mismatch, or an injected number all fail. Rubric becomes `T-01@1` | CTO, PACKET-18 |
| 233 | A grader failure on an approval note has no PRD-defined release posture | Persist the failed candidate as `status='draft'` with its integrity result and create one `EXCEPTION{note_integrity_failed}`, but create no NOTE_REVIEW and leave the claim `RESERVED`. This is deliberately stricter than a major grader's normal promotion semantics | CTO, PACKET-18 |
| 234 | PRD-08's compressed phrase 'Sign ... → PACK_READY → IN_APPROVAL' conflicts with PRD-00 §0.4 | Earlier PRD-00 controls: manifest complete plus note drafted owns `RESERVED→PACK_READY`; the officer signature owns `PACK_READY→IN_APPROVAL` in PACKET-19. PACKET-18's NOTE_REVIEW resolution returns `409 NOTE_REVIEW_UI_NOT_BUILT` and mutates nothing | CTO, PACKET-18 |
| 235 | PRD-02 routes >4M to the chairman with T-03 while PRD-08 acceptance says MD with T-03 | Preserve PRD-02 precedence; `pack.route` is configuration-only in PACKET-18 and routes nothing. PACKET-19 must not choose silently and requires an owner resolution before asserting the 4.1M scenario | CTO, PACKET-18 |
| 236 | PACKET-18's G-NOTE contract changes the numeric-claim shape and grading scope that protected PACKET-09 tests already pin | Accept `field_path` as the deprecated spelling of `{source_kind: claim_field, source_ref: <path>}` under the same comparison, and grade an artifact with no commentary nodes whole rather than skipping it. Both preserve PACKET-09 semantics without weakening any check | Builder ED-11 flag, PACKET-18 |
| 237 | The protected PACKET-18 fixture's `user:` actors are 25 characters, but PRD-00's actor transport and PRD-04 RBAC both require a 26-character ULID | Owner-side fixture correction required: lengthen `OFFICER`/`MANAGER`/`OUTSIDER` to 26-character ULIDs. Production code must not widen the actor pattern to fit a fixture | CTO fixture correction pending, PACKET-18 |
| 238 | Protected PACKET-07 `test_motor_template_registry_all_pending_capture` asserts every motor template is `pending_capture`, which PACKET-18's required `T-01: status: live` contradicts | Owner-side fixture correction required, as for #190: exclude T-01 from the all-pending assertion and pin its live body/required fields instead. The builder does not edit protected suites | CTO fixture correction pending, PACKET-18 |
| 239 | A regenerated approval note superseded its predecessor but left that predecessor's NOTE_REVIEW open, so two review items competed for one claim | Add a public `review_queue.cancel(review_id, actor, reason)` restricted to the originating `review.created` actor and `NOTE_REVIEW/approval_note`; it records no resolution or approval, emits replayable `review.cancelled{source_event_id}`, and is ledgered through the single writer. The review projection consumes/backfills that event, so exactly one NOTE_REVIEW stays open even after a rebuild (§1.9) | CTO review remediation, PACKET-18 |
| 240 | PRD-08 §3.2 does not define the generation response when the merge executes and then refuses, so a refused L3 merge was reported as a staged L1 action | Return `409 PACK_GENERATION_BLOCKED` carrying the visible EXCEPTION subtypes when no immutable version was indexed and no staged release exists; 202 is reserved for a genuinely staged action | CTO review remediation, PACKET-18 |
| 241 | The injectable renderer protocol is exactly `render(html, *, policy)`, so the binding EAT timestamp header has no separate transport | Inject the header into the source document immediately before the renderer call and include the render time in the content-addressed conversion key, so a later build never reuses a stale header. The header remains the only element permitted to differ between two builds of one source (§1.4/#227) | Builder ED-11 flag, PACKET-18 |
| 242 | ED-7a fixes the 80% and 100% coverage thresholds but not their measurement basis: whether the `platform/`+`agents/` floor is per-file or pooled, and whether branch coverage counts toward either number | Pool `platform/` and `agents/` into one figure on coverage.py's statement+branch basis, so a thin module cannot fail the build alone; enforce every `packs/**/calcs.py` individually at 100% on the same basis. Calc files are discovered by glob, and one present on disk but absent from the report fails rather than passes. Guard is `tools/ci/coverage_guard.py`, pinned by `tests/ci/test_coverage_guard.py`. A per-file floor for `platform/`/`agents/` needs a spec round | CI performance work, post-PACKET-15 |
| 243 | Page raster DPI was a `300` literal in `normalise_document`, contrary to "config over code"; PRD-01 never states a render resolution, and every ordinary test paid 300-DPI rasterisation | Move it to `page_raster_dpi` in `platform/doc_intel/defaults.yaml` and `packs/motor/doc_intel.yaml`, threaded through `model_config` exactly like `vision_text_coverage_threshold`. The production and pack value is unchanged at 300 — lowering it is an extraction-quality decision, not a CI one — and the protected acceptance scenarios still render at 300. Editable harnesses opt down to 96 through `support.doc_intel.build_doc_intel_engine`. A non-positive or non-integer DPI raises rather than falling back to the default | CI performance work, post-PACKET-15 |
| 244 | `DocIntelEngine` falls back to a real `TesseractOcrEngine` when a caller omits `ocr_engine`, so every ordinary test shelled out to the binary and CI had to install it on both test suites | Inject `support.ocr.DeterministicOcrEngine` explicitly in editable harnesses rather than monkeypatching OCR globally; run the two test jobs with `-m "not real_ocr"` and no Tesseract installed, so an omitted injection fails there instead of passing on whichever machine happens to have the binary. The real adapter keeps a dedicated `ocr-smoke` job that asserts the binary is present before running. The production fallback is unchanged | CI performance work, post-PACKET-15 |
| 245 | ED-3 mandates PostgreSQL 16 in CI but the per-test `DROP SCHEMA public CASCADE` rebuilt every application table hundreds of times per run and could not be parallelised: two xdist workers sharing one database would drop each other's tables mid-test | Build one database per worker at session start (`{base}_test_gw<n>`), load the full schema once from explicitly registered model packages, and isolate ordinary tests with `TRUNCATE ... RESTART IDENTITY CASCADE`. Suppress the repeated `build_*` `create_all` only for tables the session already created on that worker database, falling through to the real implementation for anything unregistered. Tests that assert on the schema or run Alembic carry `schema_isolated` and get a private empty database with normal behaviour. Every created, reset, or dropped name is checked by `support.database`: the configured base must end in exactly `_test`, only derived `_gw<n>`/`_iso_<token>` names may ever be dropped, and the base itself is reset but never dropped. Pinned by `tests/ci/test_database_name_guard.py`. Transaction-rollback isolation, which would also remove the truncate, needs a spec round | CI performance work, post-PACKET-15 |
| 246 | PRD-08 requires a new draft version every five seconds but the binding `note_drafts` DDL has no parent/review/idempotency columns | Store root/parent/review ids and the canonical body hash inside `body`; use the claim-wide integer version, optimistic base id/hash, and request idempotency. Add no DDL | CTO, PACKET-19 |
| 247 | A NOTE_REVIEW resolution and final signed artifact cross the review, artifact, note, event, and FSM transaction boundaries | Grade and immutably prepare the exact candidate before resolution; use the durable `review.resolved` event as an idempotent finalisation trigger, resuming from `pack.note_sign_prepared`/`pack.note_signed`/`pack.routed` evidence and never duplicating an artifact, review, or FSM hop | CTO, PACKET-19 |
| 248 | PACK_REVIEW currently authorises against `assessment.agreed_quote`, while PRD-02 requires C-08 payable with `reserve.total` fallback and the selected routing input can later change | Put the exact routing amount, field/calc version refs, and required role in the immutable approval-review payload; show/resolve it only for that exact role and return `APPROVAL_ROUTE_STALE` if recomputation differs | CTO, PACKET-19 |
| 249 | PRD-08 defines the officer Sign action but not NOTE_REVIEW Reject semantics | Reject signs nothing, returns the retained current version to `draft`, keeps `PACK_READY`, and requires a later explicit governed generation; it never silently regenerates prose | CTO, PACKET-19 |
| 250 | Manager Reject requires a new draft “pre-seeded with reasons” but does not say whether signed bytes or generated commentary may be edited | Retain the signed version and its artifacts; clone the structured body into a new `in_review` version with a separate visible `manager_rejection` metadata block, create one NOTE_REVIEW, and never splice reasons into commentary | CTO, PACKET-19 |
| 251 | PRD-08 acceptance says a KES 4.1M claim routes to MD, conflicting with earlier PRD-02's chairman band and R-12 Head-of-Claims + MD alert | Apply precedence: chairman owns approval above KES 4M; Head of Claims + MD receive T-03. While T-03 is pending capture, refuse sign/routing visibly with no artifact, review, or FSM mutation | CTO, PACKET-19; closes implementation ambiguity #235 |
| 252 | PRD-08 requires a launch ICON note field set but no ordered note-entry click path or field mapping is captured | Ship a pack-owned `icon.note_entry` slot as `pending_capture` with an empty field list and expose the blocker; executable paste-assist remains PRD-09/open item 3 | CTO, PACKET-19 |
| 253 | PACKET-18 stores immutable blob keys but defines no authenticated browser transport for the NOTE_REVIEW/S-3 viewers | Resolve an allowlisted `pack.merged` or `pack.note_signed` event id to its server-owned blob key after same-claim RBAC; never accept raw blob keys and return 404 across claims | CTO, PACKET-19 |
| 254 | PRD-04 requires Edit→Approve for every review type while PRD-08 makes the manager-visible signed artifact immutable | For PACK_REVIEW, Edit→Approve means approve with a non-empty manager annotation recorded only in the resolution event; it mutates no signed artifact and is the acceptance-trial “annotation without Reject” path | CTO, PACKET-19 |
| 255 | The authority matrix carries side effects as free text (`render T-03`) with no parsing contract, so an unrecognised verb could be silently ignored | Parse exactly `render <template-id>` in `approval_pack_agent/signing.py`; any other form refuses the signature with `409 ROUTING_BLOCKED_ON_INPUTS{blocked_on: authority_matrix.side_effects}` and creates no artifact, review, or FSM hop. A side effect that becomes unrenderable between preflight and finalisation raises `EXCEPTION{pack_route_side_effect_blocked}` and leaves the signed pack unrouted rather than skipping a mandated alert | Builder ED-11 flag, PACKET-19 |
| 256 | PRD-04 §4.3 fixes seven Claim-360 tab names while PRD-08 §8.2 requires a readiness card on Claim 360, and no tab owns it | Render the 13-row readiness card inside the existing *Documents* evidence tab. No eighth tab is invented and the protected PRD-04 tab list is unchanged | Builder ED-11 flag, PACKET-19 |
| 257 | PRD-04 requires a closed reject-reason enum that `packs/motor/review/contracts.yaml` still records as `reject_reasons: pending_capture`, but `claim_core.fsm` already binds `IN_APPROVAL→PACK_READY` to `reasons[] = {code, detail}` | `PACK_REVIEW@2` requires the existing `{code, detail}` shape with an optional `field_path`, and constrains `code` only to a non-empty string. No enum is invented; the corrected path is paired with the typed diff so PRD-03 captures a complete correction case or records `blocked_on_inputs` | Builder ED-11 flag, PACKET-19 |
| 258 | The protected PACKET-18 assertion `NOTE_REVIEW_UI_NOT_BUILT` and the protected PACKET-12 assertion `approval-pack-unavailable` both pin the absence of the surface PACKET-19 builds (register #234 anticipates the first) | Owner-side fixture correction required, as for #237/#238. Backend: `tests/acceptance/test_packet_18_approval_pack_backend.py::test_t01_values_are_exact_locked_cited_and_blocked_slots_never_gain_numbers` must resolve against `NOTE_REVIEW@2` and expect `SIGN_BLOCKED_ON_INPUTS` rather than `NOTE_REVIEW@1`/`NOTE_REVIEW_UI_NOT_BUILT`. Console: PACKET-12's assertion is preserved in code — a legacy `PACK_REVIEW` with no `approval_pack` subtype still renders the explicit unavailable state — so no console fixture change is required. The builder does not edit protected suites | CTO fixture correction pending, PACKET-19 |
