"""Shared, editable test builders.

Everything here is owned by the builder and free to change. The protected
acceptance suite under `tests/acceptance/` never imports it — those files
encode the PRD scenarios verbatim and keep their own local fixtures.
"""
